#팩토리얼 값 구하기
#양의 정수 n을 입력받아 팩토리얼 값을 구하는 프로그램

#for문
print("팩토리얼 값 계산 프로그램")

n=int(input("정수 입력: "))
fac = 1

for i in range (1,n+1,1):
  fac*=i
    
print("%d! = %d"%(n,fac))

#while문
print("팩토리얼 값 계산 프로그램")

n=int(input("정수 입력: "))
i=1
fac=1

while i<=n :
    fac*=i
    i+=1

print("%d! = %d"%(n,fac))

#함수로 표현

def factorial(n):
    fac=1
    for i in range (1,n+1,1):
        fac*=i
    return fac

print("팩토리얼 값 계산 프로그램")
n=int(input("정수 입력: "))
result=factorial(n)
print("%d! = %d"%(n,result))

#0 입력하면 종료하기
def factorial(n):
    fac=1
    for i in range (1,n+1,1):
        fac*=i
    return fac

print("팩토리얼 값 계산 프로그램")
while 1: 
    n=int(input("정수 입력: "))
    if n<=0 :
        break
    else :
        result=factorial(n)
        print("%d! = %d"%(n,result))
print("이용해주셔서 감사합니다.")
