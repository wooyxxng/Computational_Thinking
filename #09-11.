#182p 11.
#키, 몸무게 입력받아서 BMI 지수 계산하여 비만 정도 안내하는 프로그램

Height=float(input("키(m)를 입력하세요 : "))
Weight=float(input("몸무게(kg)를 입력하세요 : "))
BMI=Weight/Height**2

if BMI > 40 :
    print("고도비만 (3도 비만)")
    
elif 30 <= BMI <= 40 :
    print("비만 (2도 비만)")
    
elif 25 <= BMI < 30 :
    print("과체중 (1도 비만)")
    
elif 20 <= BMI < 25 :
    print("정상")
    
else :
    print("저체중")
