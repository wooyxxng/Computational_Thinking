#151p 03.
#이름, 나이, 키 입력받아서 정리해주는 프로그램
#키는 소수점까지 받고, 출력시에는 소수점 둘째자리까지 표시

name=input("이름이 뭐에요? ")
age=int(input("몇 살이에요? "))
height=float(input("키가 몇이에요? "))

print("이름:",name)
print("나이: %d살"%age)
print("키: %.2f cm"%height)
